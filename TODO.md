# TODO / สถานะงาน

อัปเดตล่าสุด: 2026-08-28

## 🐛 บั๊กค้างรอแก้

### ⚠️ หน้าค่าใช้จ่ายเงินเดือน (`payrollexp`): **ผู้ใช้แจ้งว่ามีจุดต้องแก้ (2026-08-28) แต่ยังไม่ได้บอกว่าจุดไหน**
เพิ่ง push ขึ้น production วันนี้ · **ถามผู้ใช้ก่อนว่าจะแก้ตรงไหน อย่าเดาเอง**
สิ่งที่มีอยู่ตอนนี้ (ไว้เทียบตอนคุย):
- อัปโหลดไฟล์ `Payroll Report_YYYY-MM_revN.xlsx` → อ่านเฉพาะชีต `Payroll Report` เก็บยอดรวมระดับแผนก
- KPI 4 ตัว + % เทียบเดือนก่อน (ค่าใช้จ่ายเพิ่ม=แดง ลด=เขียว จำนวนคน=เป็นกลาง)
- แท่งแนวนอน 2 ชุด: ตามกลุ่มธุรกิจ / ตามองค์ประกอบค่าใช้จ่าย
- กราฟเส้นแนวโน้ม (จงใจใช้เส้นไม่ใช่แท่ง เพราะยอดแกว่งแค่ไม่กี่ % รอบ ~18M แท่งที่เริ่มจากศูนย์จะดูสูงเท่ากันหมด)
- ตารางกลุ่มธุรกิจ คลิกกางดูรายแผนก + คอลัมน์เฉลี่ย/คน
- ปุ่มพิมพ์ฉบับเซ็น A4 แนวนอน 4 หน้า
- guard เช็ค 8 แถวสำคัญของ template ก่อนอัปโหลด ถ้าโครงเพี้ยนจะปฏิเสธพร้อมบอกแถวที่ผิด

### ⚠️ Headcount Report — ตารางแยกหน่วยงาน/สัญชาติ: **ผู้ใช้แจ้งว่ามีข้อต้องแก้ (2026-08-18) แต่ยังไม่ได้บอกว่าข้อไหน**
เพิ่งขึ้น production วันนี้ ยังไม่ผ่านการตรวจ — **ถามผู้ใช้ก่อนว่าจะแก้จุดไหน อย่าเดาเอง**
สิ่งที่เพิ่งทำไป (ไว้เทียบตอนคุย):
- ตาราง **"แยกตามหน่วยงาน"** ใต้ Turnover Breakdown · ปุ่มระดับ 5 แบบ: Division / Department / Section / Team / สัญชาติ
- คอลัมน์ **ต่างชาติ** ในกลุ่ม HEADCOUNT ทั้งตารางรายเดือนหลักและตารางแยก (นิยาม: สัญชาติ ≠ Thai/ไทย · ไม่ระบุ = ไม่นับ ตามกฎเดียวกับ workforce-overview.js)
- ปุ่มสลับ **รวมทั้งช่วง ↔ รายเดือน** · โหมดรายเดือน = แถวหน่วยงาน × คอลัมน์เดือน เลือกดูได้ 4 ตัวเลข (Headcount / เข้าใหม่ / ออก / ต่างชาติ)
- ท้ายแถวโหมดรายเดือน: เข้าใหม่/ออก = **"รวม"** (flow) · Headcount/ต่างชาติ = **"ล่าสุด"** (stock บวกข้ามเดือนไม่ได้)
- Export: ชีต `By {ระดับ}` ครบ 5 ระดับ + ชีตรายเดือน 4 ตัวเลขของระดับที่เปิดดูอยู่
- เทสต์ `test/headcount-breakdown.test.js` 79 เคส (ล็อกไว้ว่ายอดรวมตารางแยก = ตารางหลักเสมอ)


## 🔧 push แล้ว แต่ยังไม่ผ่านการทดสอบจากผู้ใช้ (2026-08-28)

### ระบบสิทธิ์กำหนดเองได้ (RBAC)
แอดมินสร้าง role เองได้ และติ๊กเลือกได้ว่า role ไหนเห็น/ทำอะไรได้ จากหน้า Settings
- ไฟล์ใหม่: `sql/schema_rbac.sql` (**รันแล้ว 2026-08-28** · admin 25/25 · hr 22/25 · user 11/25)
  · `js/permissions-admin.js`
- แก้: `js/app.js` (state `myPerms` + `can()` + ซ่อนเมนู + guard router) · `masterdata-admin.js`
  (เสียบการ์ดสิทธิ์บนสุดของ Settings) · `users.js` (dropdown role ดึงจาก `app_roles`)
  · `employees.js` `shift-allowance.js` `vacancy.js` `workforce-overview.js` `payroll-summary.js`
  (เปลี่ยนจาก `userRole==="hr"` เป็น `can("...")` ครบทุกจุด — ไฟล์อื่นไม่เหลือ `userRole` แล้ว)
- 3 ตาราง: `app_roles` (role เป็นข้อมูล ไม่ใช่ CHECK) · `permissions` (แคตตาล็อก 25 สิทธิ์
  แก้จากหน้าเว็บไม่ได้ตั้งใจ — เพิ่ม key ที่โค้ดไม่เช็ค = สิทธิ์หลอก) · `role_permissions` (มีแถว = อนุญาต)
- **RLS ทุกตารางเขียนใหม่ให้อ่านจากตารางสิทธิ์ผ่าน `has_perm()`** ไม่ใช่แค่ซ่อนปุ่ม
- กันพัง: `has_perm()` คืน true เสมอสำหรับ admin (ถอนสิทธิ์ตัวเองแล้วยังเข้าไปแก้คืนได้)
  · ถ้ายังไม่รัน SQL แอปถอยไปใช้กติกาเดิม (admin/hr/user) ทำงานได้ปกติ
- ⚠️ **`field.salary.read` ยังเป็นสิทธิ์หลอก** — ซ่อนได้แค่ช่องบนหน้าเว็บ
  RLS ของ Postgres คุมได้ระดับแถว ไม่ใช่ระดับคอลัมน์ · ต้องแยก `salary` ออกเป็นตารางต่างหากถึงจะปิดจริง
  (= Roadmap ข้อ 1 · งานถัดไป)
- **ยังไม่ได้ทดสอบผ่าน UI** — ต้องลองสร้าง role ใหม่แล้วดูว่าเมนู/หน้า/dropdown ทำงานถูก

### หน้าใหม่: รายงานค่าใช้จ่ายเงินเดือน (`payrollexp`)
คนละหน้ากับ `payroll` เดิม — ของเดิมโชว์ movement พร้อมเงินเดือนรายคน อันใหม่คือรายงานค่าใช้จ่ายรวมรายเดือน
- ไฟล์ใหม่: `js/payroll-summary.js` · `sql/schema_payroll_summary.sql` (**รันใน Supabase แล้ว**)
- แก้: `index.html` (เมนู + div) · `js/app.js` (router + pages) · `css/style.css` (คลาส `.pl-*`)
- **ความลับข้อมูล — ข้อกำหนดจากผู้ใช้:** อ่านเฉพาะชีต `Payroll Report` ชีตเดียว
  ชีต `Stafflist` / `BeforeProcessYDP` ที่มีเงินเดือนรายคน **ไม่ถูกอ่านและไม่ถูกอัปโหลด**
  DB เก็บแค่ยอดรวมระดับแผนก · RLS อ่าน/เขียนเฉพาะ hr+admin (ต่างจากตารางอื่นที่ให้ทุกคนที่ล็อกอินอ่านได้)
- ตัวอ่านไฟล์ยึดเลขแถวของ template ตายตัว แต่มี guard เช็ค 8 แถวสำคัญก่อนอัปโหลด
  ถ้ามีคนแทรก/ลบแถวใน template จะ**ปฏิเสธพร้อมบอกว่าแถวไหนเพี้ยน** แทนที่จะบันทึกตัวเลขผิดเงียบ ๆ
- ปุ่ม "พิมพ์ฉบับเซ็น" เปิดหน้าต่างใหม่ A4 แนวนอน 4 หน้า (สรุป+ช่องเซ็น / Operations / Sustainability+Commercial / Exploration+BKK+Legal+รวม)
- ตรวจแล้วด้วย harness (ลบทิ้งแล้ว): ตัวเลข 420 ตัวในฉบับพิมพ์ตรงกับที่สร้างด้วย Python 100% · ยอดแต่ละแผนก = คอลัมน์ Total · Expense − Deduction = Net
- **ยังไม่ได้ทดสอบจากของจริงผ่าน UI** — ต้องล็อกอินเป็น hr/admin แล้วลองอัปโหลด `Payroll Report_2026-08_rev2.xlsx`

### เครื่องมือเสริม: `tools/payroll_report_print.py`
สร้าง PDF ฉบับเซ็นจากไฟล์ Excel ตรง ๆ ไม่ต้องผ่านแอป — `python3 tools/payroll_report_print.py "<ไฟล์>.xlsx"`
(ต้องมี openpyxl · เครื่องนี้ยังไม่มี Node/bun ถ้าจะใช้ `/design` ต้องลง Node ก่อน)

## 🔍 รอผู้ใช้ตรวจตัวเลขจริง (ยังไม่มีใครยืนยัน)
Dashboard ใหม่ + กฎวันที่ push ขึ้น production แล้ว (commit `e7ebf63`) แต่ยังไม่ได้เทียบกับข้อมูลจริง:
- เดือน **ก.ค. พ้นสภาพ +1 / ส.ค. −1** เทียบกับก่อนเปลี่ยนกฎ
- Dashboard / Headcount Report / Movement Report / Workforce Overview ต้องได้เลข **ตรงกันทั้ง 4 หน้า**
- Console ไม่มี error

## ✅ เสร็จ + push ขึ้น production แล้ว

### ซ่อม status ที่เป็นชื่อจังหวัด — รันแล้ว ✅ (2026-08-27)
- เจอ 4 คน: `AKR26071368` Nakhonsawan · `AKR26071370` Phichit · `AKR26071371` Phitsanulok · `AKR26071372` Phichit
  (emp_code ติดกัน …1368/1370/1371/1372 — น่าจะมาจากไฟล์ import ชุดเดียวที่คอลัมน์เลื่อน)
- ทุกคน `end_date` = NULL และ **ช่อง `province` มีค่าถูกต้องอยู่แล้ว** จึงไม่มีข้อมูลจะเสีย → ตั้งเป็น `Active` ได้เลย
- ผลหลังรัน: Active 491 · Resigned 77 · Terminated 28 · Retired 4 (รวม 600) — ไม่มีชื่อจังหวัดเหลือ
- `sql/fix_employee_status.sql` ปรับให้ **รันทั้งไฟล์ได้และรันซ้ำได้** เผื่อเจออีก:
  - 2.1 เก็บค่าเดิมลง `remark` *เฉพาะแถวที่ `province` ว่าง* (ถ้า province มีค่าแล้วไม่ต้องเก็บ จะได้ไม่เปื้อนเปล่า ๆ)
  - 2.3 ไม่มี `end_date` → `Active`
  - 2.4 มี `end_date` → ดึงเหตุผลจากตาราง `movements` มาเติมเอง (Resignation→Resigned ฯลฯ) ไม่ต้องไล่ทีละคน
  - 2.2 (ย้ายค่าไป `province`) ยัง comment ไว้ — เป็นการตัดสินใจว่าค่าที่ปนมาเป็นจังหวัดจริงไหม ต้องดู STEP 1.1 ก่อน

### Dashboard ผู้บริหาร + รวมกฎวันที่ (2026-08-11, commit `e7ebf63`)
- **Dashboard เขียนใหม่ทั้งหน้า** สไตล์ Premium Executive (navy/ทอง, การ์ดมุมโค้ง 16px, design tokens `--exec-*`)
  1. KPI: การ์ด Headcount เด่น + เทียบเดือนก่อน (จำนวน+%) + progress เทียบแผน · เข้าใหม่ / พ้นสภาพ / อัตราลาออก
  2. ข้อมูลเชิงลึกกำลังคน + กำลังพ้นสภาพ 30/60/90 วัน
  3. กราฟแนวโน้ม 6 เดือน — เส้นกำลังคน + แท่งเข้า/ออก + เส้นประแผน + tooltip
  4. Waterfall (ยกมา+เข้า−ออก=ยกไป) + แผนก Actual/Plan/Gap (เกณฑ์ ครบ/เฝ้าระวัง ≤10%/วิกฤต >10%) + ประเภทสัญญา
  5. Timeline ความเคลื่อนไหว + ปุ่มดูทั้งหมด
- **Approved Plan** มาจากตาราง `position_quota` (โหลดเข้า state กลาง `allPosQuota`)
- **กฎวันที่รวมไว้ที่ `js/app.js` จุดเดียว** — `sepYM` / `isActiveAtMonthEnd` / `hcAtMonthEnd` / `lastDayOfMonth`
  ทุกรายงาน import ไปใช้ (เดิมก๊อป 4 ไฟล์ = ต้นเหตุ regression 8 ครั้ง)
  **termination 1/8 → ทำงานถึง 31 ก.ค. → พ้นสภาพนับ ก.ค. และหลุดจาก headcount ก.ค.** (ยืนยัน 2026-08-03)
- เทสต์ `test/headcount-date.test.js` (46) + `test/dashboard.test.js` (33) — ดึงฟังก์ชันจริงจาก app.js มารัน
- `index.html` ใส่ `css/style.css?v=N` กัน browser cache CSS เก่า (แก้ CSS แล้วหน้าไม่เปลี่ยน = บวกเลขนี้)

### เอกสารแนบ + กันบันทึกซ้ำ + ค้นหา (2026-08-18, commit `a9e1427`) ✅
- **แนบเอกสารใน Staff Movement** — เก็บใน Supabase Storage bucket `movement-docs` แบบ private,
  เปิดดูผ่าน signed URL อายุ 60 วินาที · อัปโหลด/ลบเฉพาะ HR+Admin · ไฟล์ไม่เกิน 10 MB
  · ตารางแสดง 📎 กดเปิดได้ · แก้ไขรายการเดิมเปลี่ยน/ถอดไฟล์ได้
  · ต้องรัน `sql/schema_movement_attachment.sql` (รันแล้ว 2026-08-18)
- **กันบันทึกการพ้นสภาพซ้ำ** — คนที่มี Resignation/Termination/Retirement อยู่แล้ว บันทึกซ้ำไม่ได้
  แจ้งชื่อ + วันที่มีผล + คนบันทึกของรายการเดิม (แก้รายการเดิมยังทำได้ปกติ)
- **ค้นหาพนักงาน** — ครอบคลุมทุกฟิลด์ในตาราง + พิมพ์หลายคำได้ (ต้องเจอครบทุกคำ) + ปุ่ม × ล้างคำค้น
- **กัน Status เพี้ยน** — import รับเฉพาะ Active/Resigned/Terminated/Retired/Transferred
  ค่าอื่นจะไม่เขียนทับและเด้ง toast บอกจำนวนที่ข้าม (ต้นเหตุที่เคยมีชื่อจังหวัดโผล่ในช่องสถานะ)

### แก้ Position Quota บันทึกไม่เข้า (2026-08-11, commit `e9bc0c7`) ✅
- **ต้นเหตุ:** ตาราง `position_quota` สร้างมือใน Supabase โดย**ไม่มีคอลัมน์ `fiscal_year`** แต่ `js/vacancy.js`
  ส่ง `fiscal_year` ไปทุกครั้ง → Postgres ปฏิเสธ insert ทั้งหมด → ตารางว่าง 0 แถวมาตลอด
  (และทำให้ Dashboard ขึ้นว่า "ยังไม่ได้ตั้งแผนอัตรากำลัง")
- **ที่ทำให้หายาก:** `loadPosQuota()` กลืน error เงียบ ๆ ไม่แจ้งอะไรเลย — แก้แล้วให้เด้ง toast + log
- **แก้:** `sql/schema_position_quota.sql` เติมคอลัมน์ที่โค้ดใช้ + unique index + RLS (อ่าน=authenticated,
  เขียน=hr/admin ให้ตรงกับที่ UI จำกัดไว้ เดิมเป็น `Allow all for authenticated` ซึ่งหลวมเกิน)
- รันใน Supabase แล้ว ทดสอบเพิ่ม quota ผ่านหน้าเว็บได้จริง (ยืนยัน 2026-08-11)

### สิทธิ์ระดับฐานข้อมูล — รันใน Supabase แล้ว ✅ (ยืนยัน 2026-08-11)
- `movements_update` / `movements_delete` = `created_by = auth.uid() OR get_my_role() = 'admin'`
- `roles_update` / `roles_delete` = `get_my_role() = 'admin'` (HR/user แก้สิทธิ์ admin ไม่ได้แล้ว)

### Staff Movement + Admin controls (2026-07-31, commit `5375873`)
- **Import Excel ปรับตำแหน่งหลายคน (bulk)** หน้า Staff Movement (HR/Admin) — สร้าง movement + อัปเดตพนักงาน (ตำแหน่ง/แผนก/**job_level** จากคอลัมน์ "ระดับใหม่") · รองรับ date serial ของ Excel · helper ร่วม `empUpdateFromMovement`
- **ปุ่มลบทุกแถว** ใน Staff Movement (admin ลบ/แก้ได้ทุกรายการ)
- **User Management เฉพาะ Admin** (guard) + RLS กัน HR/user แก้ role
- **ไม่เด้งกลับ Dashboard** เมื่อ token refresh/สลับแท็บ (boot ครั้งเดียวต่อ session — `appBooted`)
- **Export พนักงานเพิ่มคอลัมน์ Province** (เดิมหาย ทำ column เลื่อนไปปนกับ Status)

### กระดิ่งแจ้งเตือน (Notification Bell)
- กระดิ่งมุมขวาบน sidebar + badge นับที่ยังไม่อ่าน + dropdown panel
- **เก็บใน Supabase (ตาราง `notifications`) + realtime — shared ทุก user** (เดิม localStorage แยกเครื่อง user อื่นไม่เห็น) commit `29187af`
- เด้งเฉพาะ "เพิ่มใหม่": Movement, พนักงาน, Import, Position Quota, Master data
- **Proactive alerts** — สัญญาใกล้หมด / พ้นโปร / ใกล้เกษียณ (กันแจ้งซ้ำแบบ global ด้วย `dedup_key`)
- ปุ่ม "อ่านแล้วทั้งหมด" (สถานะอ่านเป็นราย browser, ไม่ลบ log ของคนอื่น)

### คำนวณค่ากะ (Shift Allowance) — หน้าใหม่ HR/Admin
- อัปโหลด Excel (sheet Clean_Data) → คำนวณตาม `shift_allowance_calculation_spec.md` (Pass1 นับตระกูลกะ→เรต, Pass2 pro-rate รายวัน)
- **กรองระดับ O** (O1/O2/O3) อัตโนมัติ จาก job_level ใน DB — match Employee_ID↔emp_code
- **ยกเว้นรายคน (grandfather)** — checkbox "ได้รับค่ากะ (กำหนดเอง)" ในฟอร์มพนักงาน (คอลัมน์ `shift_allowance_override`) สำหรับ S/M ที่ HR ให้ต่อ → badge "พิเศษ" commit `511c646`
- **เก็บประวัติรายเดือน** ตาราง `shift_allowance` (upsert emp_code+month) + แท็บดูย้อนหลัง
- แจ้งเตือน: ไม่พบใน DB / ไม่ใช่ระดับ O / วัน CHECK_NOTE

### คำนวณค่ากะ — กติกาที่สรุปแล้ว (2026-08-18) ⭐
เทียบกับ "เฉลยที่คิดมือ" ของ ก.ค. 2026 จนตรงทั้งหมด · ทุกข้อผู้ใช้ยืนยันเอง · รายละเอียด+เหตุผลอยู่ใน `CLAUDE.md`
- **อัตรารายวัน = อัตราเดือน ÷ 30 คงที่** (ไม่ใช่วันจริงในเดือน) ยอดรวมไม่เกินอัตราเดือน → ก.ค. 31 วัน = 1,800 (ติดเพดาน) · ก.พ. 28 วัน = 1,680
- **ตัดวันนอกช่วงการจ้าง** (ก่อน `join_date` / ตั้งแต่ `end_date`) — ยอดถูกต้องก็ต่อเมื่อวันในระบบพนักงานถูกต้อง หน้าเว็บลิสต์คนที่ถูกตัดให้ตรวจ
- **N03 กะเดี่ยว = 1,200** (ปกติกะเดียว = 0) เก็บที่ `master_shift_codes.solo_rate` เพิ่มกะยกเว้นอื่นได้เองไม่ต้องแก้โค้ด
- **NOR + N03** → จ่ายเฉพาะวันที่เข้า N03 · แต่ถ้า N03 + วันหยุด/ลาที่จ่ายได้ **เกิน 15 วัน** → จ่ายเต็มอัตราไปเลย
- **NOR + F01** → ไม่คิดค่ากะ (เช็คจากรหัสตรง ๆ ไม่ผ่านตระกูล)
- **สาย Process เข้า N03** → วันนั้นไม่จ่าย (วันกะอื่นจ่ายปกติ, อัตราไม่เปลี่ยน) จับจากคำ `process` ในสังกัด
- **หมายเหตุบอกว่าไม่ได้เงิน ชนะทุกอย่าง** — `ไม่จ่ายเงิน`/`ไม่รับค่าจ้าง`/`หักเงิน` ในช่องหมายเหตุ → ไม่จ่าย แม้ติด `Leave_No_Deduct`
- **ลาป่วยติดกัน ≥3 วัน = ไม่จ่ายวันลานั้น** · **วันหยุดประจำสัปดาห์ที่คั่นกลางไม่ตัดช่วง** (ป่วย-ป่วย-หยุด-ป่วย = 4 วันติด) แต่ตัววันหยุดยังจ่าย
- **พักงาน** = ไม่จ่ายวันนั้น (มาก่อนทุกเงื่อนไข) · **ลาไม่รับค่าจ้าง / ขาดงาน** = ไม่จ่าย (เดิมถูกอยู่แล้ว)
- **ไม่มีวันทำงานจริงเลยทั้งเดือน** = ฿0 จนกว่า HR จะกด **อนุมัติจ่ายรายคน** (ปุ่มเดียวกันคืนวันลาป่วยยาวได้ด้วย)
- พักงาน/ลาป่วย อ่านจากช่อง **หมายเหตุ** แบบจับคำ (ไฟล์จริงไม่มีคอลัมน์เฉพาะ) — หน้าเว็บลิสต์ข้อความทุกอันที่ตีความให้ตรวจ

### เครื่องมือตรวจสอบค่ากะ (2026-08-18)
- **เทียบกับเฉลยอัตโนมัติ** — ไฟล์ลงเวลาที่มีคอลัมน์ `Shift_Allowance` เทียบให้เอง · หรือกด **📋 เทียบกับไฟล์เฉลย** อัปโหลดไฟล์สรุปแยก (เดาชื่อคอลัมน์ไทย/อังกฤษให้)
- **บอกสาเหตุที่ไม่ตรง** รายแถว — ตัวหารต่างกัน / จำนวนวันไม่ตรง (+ชี้ `join_date`/`end_date`) / เกณฑ์กะเดียว / เฉลยไม่จ่าย
- **เตือนรหัสกะที่ไม่รู้จัก** + เพิ่มเข้า DB ได้จากหน้าเว็บทันที แล้วคำนวณใหม่ให้เลย (เดิมข้ามเงียบ ๆ ทำให้ยอดหาย 600/คน)
- แถบให้ตรวจ: 🏭 Process · 🛑 รออนุมัติ · 🩺 ลาป่วยยาว · ✂️ วันนอกช่วงจ้าง · 🔎 ข้อความหมายเหตุ · 🚨 รหัสกะไม่รู้จัก
- Export เพิ่มชีต "ไม่ตรงกับเฉลย" + คอลัมน์สาเหตุ · ชีตรายวันมีสถานะ `OUT_OF_PERIOD` / `(ลาป่วยยาว-ไม่จ่าย)`
- **เทสต์ `test/shift-allowance.test.js` — 199 เคส** รันด้วย `osascript -l JavaScript test/shift-allowance.test.js` (ดึงฟังก์ชันจริงจาก source มารัน ไม่ก๊อปตรรกะ)

### แก้บั๊ก
- **ปุ่มอัปโหลดหน้าค่ากะกดไม่ได้** — สั่ง `.click()` บน `input[type=file]` ที่ `display:none` ซึ่ง Safari บล็อก → เปลี่ยนเป็น `<label for>` (หน้าอื่นที่ import Excel ยังใช้ท่าเดิม ถ้าเจอปัญหาเดียวกันแก้แบบนี้ได้)
- realtime subscribe ซ้ำ (`cannot add postgres_changes...`) — guard ด้วย `realtimeChannel`
- SQL scripts idempotent (รันซ้ำไม่ error) commit `db873c4`

## ⚠️ ต้องทำใน Supabase (ถ้ายังไม่ได้ทำในโปรเจกต์จริง)
รันใน SQL Editor (ทีละไฟล์, idempotent):
1. ✅ `sql/schema_notifications.sql` — ตารางกระดิ่ง shared
2. ✅ `sql/schema_shift_allowance.sql` — ตารางประวัติค่ากะ + คอลัมน์ `shift_allowance_override` ในตาราง employees
3. ✅ `sql/schema_admin_permissions.sql` — รันแล้ว ยืนยันด้วย pg_policies เมื่อ 2026-08-11
4. ✅ `sql/schema_position_quota.sql` — เติมคอลัมน์ fiscal_year ฯลฯ + RLS hr/admin (รันแล้ว 2026-08-11)
5. ✅ `sql/schema_movement_attachment.sql` — คอลัมน์ไฟล์แนบ + storage bucket + สิทธิ์ (รันแล้ว 2026-08-18)
6. ✅ `sql/fix_employee_status.sql` — ซ่อม status ที่เป็นชื่อจังหวัด (รันแล้ว 2026-08-27 · แก้ 4 แถว)
7. ✅ `sql/schema_shift_codes.sql` — ย้ายรหัสกะมาเก็บใน DB เพิ่มกะใหม่ได้จากหน้าเว็บ (รันแล้ว 2026-08-18 · ยืนยัน 14 รหัส: DAY 6, AFT 3, NIT 5)
8. ✅ `sql/schema_shift_codes_solo.sql` — คอลัมน์ `solo_rate` + ตั้ง N03 = 1,200 (รันแล้ว 2026-08-18)
9. ✅ `sql/schema_payroll_summary.sql` — ตาราง `payroll_period` + `payroll_summary` (ยอดรวมเงินเดือน)
   RLS อ่าน/เขียนเฉพาะ hr+admin (รันแล้ว 2026-08-27 · ยืนยันว่า anon อ่านไม่ได้และเขียนไม่ได้)
10. ✅ `sql/schema_rbac.sql` — ระบบสิทธิ์กำหนดเองได้ + เขียน RLS ทุกตารางใหม่ให้ใช้ `has_perm()`
    (รันแล้ว 2026-08-28 · ยืนยัน RPC `my_permissions`/`has_perm` เรียกได้ และ anon เขียนสิทธิ์ไม่ได้)

## 🗺️ Roadmap (ยังไม่เริ่ม เรียงตามความสำคัญ)
1. **แยกตาราง `salary`** — ยังเป็นช่องโหว่เดิม: ใครล็อกอินก็ดึง `salary` ผ่าน API ได้ (สำคัญสุด)
   ตอนนี้มีสิทธิ์ `field.salary.read` คุมการแสดงผลแล้ว แต่ RLS คุมระดับคอลัมน์ไม่ได้
   ต้องย้าย `salary` ไปตาราง `employee_salary` ที่มี RLS ของตัวเอง แล้วแก้ import/export/ฟอร์มพนักงาน
2. **pending_roles + จำกัดการสมัคร** — กัน user ลบของคนอื่น, จำกัดโดเมนอีเมล/เชิญโดย admin
3. **Audit log ถาวรใน DB** — ตาราง activity log (ใครแก้/เพิ่ม/ลบ + ค่าเดิม/ใหม่)
4. **Validation + DB constraints** — ลำดับวันที่, เงินเดือนห้ามติดลบ, status/type ในลิสต์
5. **Movement + Employee update เป็น transaction เดียว** (ย้ายไป Supabase RPC)
6. **Automated tests ขั้นต่ำ** — business logic (headcount, effective date, alerts) + RLS
7. **ต่อยอด** — Employee profile + timeline, Global search, Analytics (turnover rate, headcount trend)

## 📝 เรื่องค้าง/ยังไม่ทดสอบเต็ม
- **การอนุมัติจ่ายรายคน (ลาป่วยยาว / ไม่มีวันทำงาน) ยังไม่เก็บลง DB** — อยู่ในหน่วยความจำหน้าเว็บเท่านั้น อัปโหลดไฟล์ใหม่แล้วรีเซ็ต ต้องอนุมัติแล้วกด 💾 บันทึกในรอบเดียวกัน · ถ้าจะให้จำข้ามรอบต้องเพิ่มตาราง/คอลัมน์
- `F01` ยังไม่มีใน `master_shift_codes` — ถ้าเจอคนใช้ F01 **ปนกับกะอื่น** (ไม่ใช่คู่ NOR+F01) ต้องเพิ่มเข้าระบบ ไม่งั้นนับตระกูลขาด (น่าจะเป็นกะเช้า เพราะ F03/F04 เป็นเช้า)
- ตรวจว่ารายชื่อ "สาย Process" 200 คน (จับคำ `process` ในสังกัด) ครอบคลุมถูกต้อง — ทั้งแผนก Processing ถูกจับหมด
- §7.3 ลาเศษวัน = คิดเต็มวัน (ตาม pseudocode ที่ผู้ใช้เลือก 2026-07-20)

## หมายเหตุ (ต่อเครื่องอื่น)
- ประวัติแชต Claude Code เก็บ local ที่ `~/.claude/projects/` ไม่ sync ข้ามเครื่อง — เปิดอีกเครื่อง `git pull` แล้วอ่านไฟล์นี้ต่อได้
- Git identity ของ repo ตั้ง local เป็น Kopbun Tungkasen <kopbun@akararesources.com>
- Local test server: `python3 -m http.server 8000 --directory "<repo>"` แล้วเปิด http://localhost:8000 (login ต้องผ่าน http ไม่ใช่ file://)
- ⚠️ GitHub token ที่เคย push เคยโพสต์ในแชต — ถ้ายังไม่ regenerate ควรทำที่ https://github.com/settings/tokens
